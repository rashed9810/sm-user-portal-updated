import Topbar from '@/components/Topbar';
import React from 'react';

const Topbar_title = ({ children }: any) => {
    return (
        <div>
            {/* <Topbar children={children} /> */}
        </div>
    );
};

export default Topbar_title;